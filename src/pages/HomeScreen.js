import React from "react";
import { Animated, SafeAreaView, View } from "react-native";

export default function HomeScreen() {
  return (
    <SafeAreaView>
      <Animated.View>HomeScreen</Animated.View>
    </SafeAreaView>
  );
}
